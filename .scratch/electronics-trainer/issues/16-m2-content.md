# 16: М2 — контент и приёмка

**What to build:** Полный контент Модуля «Компоненты»: Теория (диод и светодиод, конденсатор и RC-цепочка, транзистор как ключ, потенциометр), 12–16 Вопросов с Разборами на каждый вариант, 5–7 Схема-заданий с Подсказками, Экзамен М2, объединяющий темы (например, схема с кнопкой, транзистором и светодиодом с RC-задержкой). Приёмка — smoke-проход М1 → М2 с чистого Прогресса и продолжением с экспортированного.

**Blocked by:** 14 (переходные процессы), 15 (транзистор, потенциометр, зуммер)

**Status:** done

- [x] Объёмы контента М2 на месте: ~4 темы Теории, 12–16 Вопросов, 5–7 Схема-заданий, Экзамен
- [x] У каждого Вопроса — Разбор к каждому варианту; у каждого Схема-задания — две Подсказки
- [x] Автотест-линтер контента М2 проходит без заглушек
- [x] Smoke-проход М1 → Экзамен М2 выполнен вручную без ошибок
- [x] М3 остаётся заблокированной до прохождения М2

## Comments

Implemented in feat commit + рефактор-коммит по итогам code-review + сквозной smoke-проход в браузере (404 → 414 тестов). Layout:

- `src/content/m2.ts` — М2 добран до полного объёма: 13 Вопросов (8 из волн 13–15 + 5 новых, каждый — с Разбором на каждый вариант и Подсказками): числовые «ток через диод после порога» ((5−0,7)/430 = 10 мА) и «номинал резистора зуммера для 60 мА» (100 Ом), выбор «какой светодиод не засветится от 2,5 В» (синий: порог 3 В), «τ при увеличении R в 4 раза» (τ = R·C — прямая пропорциональность), «зачем резистор в базе ключа» (без него ток базы неограничен). Временный Экзамен из М1 (моторчик) заменён настоящим Экзаменом М2 по примеру из тикета: кнопка замыкается в t = 0,5 с, RC-цепь в базе растягивает включение транзисторного ключа со светодиодом. Условия (12, все — измерения): использование батареи/кнопки/2+ резисторов/конденсатора/транзистора/светодиода; τ = 8–12 с; V_C(t=1 с) = 0,3–0,6 В (задержка ещё идёт, светодиод не горит); V_C(t=3 с) = 0,6–0,9 В (конденсатор у порога открытия — переход база—эмиттер держит его на ≈0,7 В, что и означает «задержка прошла, база открыта»; это косвенное измерение факта открытия, сформулировано прямо в условии и Подсказках); светодиод светится, ток 5–15 мА; ток через кнопку малый 50 мкА–5 мА. Эталон: база 100 кОм (τ = 10 с), коллектор 1 кОм, конденсатор 100 мкФ → Симулятор даёт ровно τ = 10 с; 439 мВ в t = 1 с; 701 мВ в t = 3 с; светодиод 6,93 мА (ключ в насыщении); кнопка 83 мкА. `module2Palette` — явный список Палитры Модуля для Экзамена (аналог module1Palette у М1), равенство объединению Палитр Заданий держит линтер.
- `src/domain/simulator.ts` — показание Компонента при решении с переопределёнными контактами: `readingOfComponent` звал `branchResistance(branch)` без `options.contactStates`, поэтому коммутатор, переключённый переходным режимом, читался по нарисованному состоянию (узловые потенциалы — от решённой топологии, а ток — от нарисованной: у Экзамена ток через нарисованную разомкнутую кнопку выходил 0 А вместо 83 мкА). Фикс в одну строку + golden-тест в `simulator.test.ts`: нарисованный разомкнутым выключатель с `contactStates: closed` читается током 9 мА и напряжением ≈9 мкВ. До Экзамена это не проявлялось: никто не измерял ток коммутатора, нарисованного разомкнутым в Задании с переходным режимом.
- `src/content/contentLint.test.ts` — секция «Объём контента М2 (тикет 16)»: теория ≥ 4 тем, 12–16 Вопросов обоих видов, 5–7 Схема-заданий + ровно один Экзамен; отдельный тест «Экзамен объединяет темы Модуля» (кнопка + транзистор + светодиод + конденсатор + резисторы + условия во времени); равенство `modulePaletteOf(module2)` = `module2Palette`. Эталон-решения Экзамена через главный шов `evaluate`: положительное (100 кОм/1 кОм/100 мкФ) + три отрицательных (ёмкость в 10 раз меньше — срываются и τ, и «задержка идёт в t = 1 с»; нарисованная замкнутой кнопка — в t = 0,5 с разомкнётся; резисторы по умолчанию 1 кОм — τ на два порядка меньше границ). Секция заглушек уже покрывала М2 целиком через `course`.
- `src/components/App.test.tsx` + `src/testing/navigation.ts` — сквозной поток «М1 сдана → М2» на 20 Заданий: 13 Вопросов в порядке очереди, все Схема-задания и новый Экзамен, собираемый на Холсте кнопками (хелпер `wireDelayKey` переиспользует `wireTransistorKey` и довязывает конденсатор база→«минус»); счётчики «0 из 20» в тестах Курса/экспорта-импорта.
- М3 и блокировка: М3 в Курсе пока нет (волна тикетов 17–19) — держать её закрытой будет тот же линейный механизм `blockingModuleOf` (следующий Модуль открыт, только когда пройден предыдущий; тесты в `course.test.ts`). В smoke-проходе правило подтверждено на живом Курсе: с чистого Прогресса М2 — «Заблокирован» (disabled) с подписью «Откроется после Модуля «Основы DC»», после М1 — открыта; после «Начать заново» — снова заблокирована.

Code-review (Standards + Spec, два параллельных агента): жёстких нарушений нет — идентификаторы английские, термины CONTEXT.md без слов из Avoid-списков, условия Экзамена — только измерения (ADR-0001). Исправлено: док-комментарий М2 противоречил сам себе (старый абзац «у Палитры нет отдельного списка» рядом с новым `module2Palette`) — переписан. Осознанно оставлено: слово «эталон-решения» — устоявшийся словарь самих тестов контента со времён тикета 12 (`m1.ts`, заголовки секций contentLint), к проверке ученика отношения не имеет; локальный делегат `circuitTaskOf` и повтор трёхстрочного паттерна «evaluate → не correct» в тестах — стиль существующих секций тикетов 13/15. Spec-агент подтвердил: объёмы сходятся, скоуп-крипа нет (module2Palette и wireDelayKey — оправданные следствия Экзамена), фикс Симулятора — единственное место, игнорировавшее переопределение контактов (строки 555/763 это уже учитывали), физика Экзамена ложных отказов корректных решений не даёт.

Smoke-проход вручную (IAB, vite dev, 1280×720): с чистого Прогресса — М2 «0 из 20» и «Заблокирован»; М1 сеяна (22/22) через хранилище, как в App-тестах. М2 полностью руками в UI: 6 карточек Теории → 13 Вопросов отвечены верно (все «решено с первой попытки») → «зажги светодиод» (7,14 мА) → «диод» → «заряди конденсатор» (выключатель сам замкнулся, 19 кОм) → «ключ на транзисторе» (10 кОм в базу, кнопка замкнута) → «делитель» (движок 50 % → «работает, но не по условию» не словили: сразу 60 % → 3,6 В) → «зуммер» (100 Ом) → Экзамен: батарея, кнопка, 100 кОм, транзистор, 1 кОм, светодиод, конденсатор с базы на «минус» — «Пройдено», все 12 измерений с числами, совпадающими с golden-тестами до знака. Модуль завершён: «20 из 20 · с первой попытки: 20», Экзамен «решён с первой попытки». Продолжение с экспортированного: «Экспорт в файл» отдаёт файл (событие download; содержимое из IAB не читается — для импорта файл того же формата собран по документированному формату backup), «Начать заново» с подтверждением — «0 из 20», М2 снова заблокирована, импорт файла — «Прогресс и схемы Песочницы восстановлены», М2 «20 из 20» с «Повторить». Ошибок страницы за весь проход — ноль (коллектор window.onerror/unhandledrejection); тестовые данные из хранилища убраны.
